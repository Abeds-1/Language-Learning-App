<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"> <!-- Transparent background -->

    <!-- Box with rounded corners in the center -->
    <LinearLayout
        android:id="@+id/permissionBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="center" 
        android:background="@drawable/rounded_greeting_box" 
        android:padding="32dp"
        android:gravity="center"
        android:layout_marginLeft="16dp"  
        android:layout_marginRight="16dp">

        <!-- Text asking to turn on the service -->
        <TextView
            android:id="@+id/textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Would you like to turn on the service?"
            android:textSize="18sp"
            android:textColor="@color/color_on_primary"
            android:layout_marginBottom="20dp"/>

        <!-- Yes button -->
        <Button
            android:id="@+id/btnYes"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Yes"
            android:backgroundTint="@color/color_primary"
            android:textColor="@android:color/white"
            android:layout_marginBottom="16dp"/>

        <!-- No button -->
        <Button
            android:id="@+id/btnNo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No"
            android:backgroundTint="@color/color_primary"
            android:textColor="@android:color/white"/>
    </LinearLayout>
</FrameLayout>
